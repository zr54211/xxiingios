# Сторонние компоненты

## zxing-cpp (deps/zxing-cpp, git submodule, тег v3.1.0)

- Лицензия: Apache License 2.0 (`deps/zxing-cpp/LICENSE`)
- Статически линкуется в бинарники компоненты.
- Обязательства при распространении: включать копию лицензии в поставку
  (упаковщик `package/make-zip.ps1` кладёт `LICENSE-zxing-cpp.txt` в zip);
  при модификации файлов библиотеки — помечать изменения (сейчас изменений нет,
  используется немодифицированный тег).

## Заголовочные файлы 1С Native API (vendor/1c-native-api)

- Собственность фирмы «1С», распространяются в составе комплекта
  «Технология внешних компонент» для разработки компонент.
- См. `vendor/1c-native-api/README.md` (TODO: сверка с официальным комплектом).
